import styles from './App.module.scss'

const App = (): JSX.Element => {
  return (
    <div className={styles.App}>
      <h1 className={styles.caption}>Hello React!</h1>
    </div>
  )
}

export default App
